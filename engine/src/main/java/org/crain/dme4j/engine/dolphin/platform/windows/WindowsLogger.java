package org.crain.dme4j.engine.dolphin.platform.windows;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

class WindowsLogger {

    // Probably a better way to group loggers for profiles. Need to review SLF4J
    static final Logger WINDOWS_LOGGER = LoggerFactory.getLogger(WindowsLogger.class);
}
